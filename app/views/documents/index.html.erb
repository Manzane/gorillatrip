<h1>Liste de tes documents</h1>
<p></p>

<% @documents.each do |document| %>
  <div>
    <%= document.name %>
    <%= document.doc_type %>
    <% if document.type == "passport" %>
      <%= l travel.expiration_date %>
      <% end %>
    Ici le document attaché
    <%= link_to edit_document_path(document) do %>
      <p>Modifie ce doc </p>
    <% end %>
    <%= link_to document_path, method: :delete, data: { confirm: "Es-tu certain?" } do %>
      <p>Supprime ce doc</p>
    <% end %>
  </div>
<% end %>

<%= link_to new_document_path do %>
  <p>Ajoute un nouveau document </p>
<% end %>
